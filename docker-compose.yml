services:
  redis:
    image: redis:latest
    ports:
      - 6379:6379
    volumes:
      - redis_data:/data
    command: redis-server
    networks:
      - app-network
  auth_service:
    build: auth_service
    ports:
      - 8081:8081
    networks:
      - app-network
    depends_on:
      - redis
    command: ["build/auth_service"]
  api:
    build: api
    ports:
      - 8080:8080
    networks:
      - app-network
    command: ["build/ep_api"]

networks:
  app-network:
    driver: bridge

volumes:
  redis_data:
